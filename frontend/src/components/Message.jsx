import React from 'react'
import { useState, useEffect } from 'react'
import api from '../api'

// This component relies on parent to render the chat log; we keep a placeholder or future expansion.
export default function Message() {
  return null
}
